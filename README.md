## How To Use

1. Install Rust [Rust Page](https://www.rust-lang.org/tools/install).

1. Install Dependencies:

```shell
$ cargo install mdbook mdbook-toc mdbook-admonish
```
1. Serve:

```shell
$ mdbook serve -o
```